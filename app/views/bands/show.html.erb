<h1> <%= @band.name %> </h1>

<p> Current band leader: <%= user_link(@band_leader) %>

<p> <%= @band.description %> </p>

<h3> Related songs </h3>

<%= render partial: 'common/list_songs', locals: {songs: @band.songs} %>

